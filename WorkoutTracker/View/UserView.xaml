<UserControl x:Class="WorkoutTracker.View.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkoutTracker.View" xmlns:viewmodel="clr-namespace:WorkoutTracker.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:MainWindowViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
            <TextBox Text="{Binding UserVM.User.UserName}" FontSize="34" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Joined" FontSize="24" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding UserVM.User.DateJoined, StringFormat='{}{0:MMMM dd, yyyy}'}" FontSize="24"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="1" HorizontalAlignment="Center">
            <TextBlock Text="Goals" FontSize="24"/>
            <ListView FontSize="24" DataContext="{Binding UserVM}" ItemsSource="{Binding User.FavoriteExercises}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="25"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding ExerciseName}"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" >
                                <TextBlock Text="{Binding TargetWeight}" />
                                <TextBlock Text="kg" Margin="5,0,0,0" />
                            </StackPanel>
                            <Button Content="D" Grid.Column="2"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ComboBox with available exercises" FontSize="24" Margin="0,0,10,0"/>
                <TextBlock Text="Weight goal" FontSize="24" Margin="0,0,10,0"/>
                <Button Content="Add" FontSize="24" HorizontalAlignment="Right" VerticalContentAlignment="Top"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="2" HorizontalAlignment="Center">
            <TextBlock Text="Personal Records" FontSize="24"/>
            <ListView FontSize="24" DataContext="{Binding UserVM}" ItemsSource="{Binding PersonalRecords}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding ExerciseName}"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" >
                                <TextBlock Text="{Binding MaxWeight}" />
                                <TextBlock Text="kg" Margin="5,0,0,0" />
                            </StackPanel>
                            <TextBlock Text="{Binding DateAchieved, StringFormat='{}{0:d/M-yy}'}" Grid.Column="2"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <Button Content="Save" Command="{Binding UserVM.SaveUserCommand}" Width="150" FontSize="18" BorderThickness="1.5" Padding="10" HorizontalAlignment="Center" Grid.Row="3" Margin="0,0,0,15" />

        <Button Content="Delete" Command="{Binding UserVM.DeleteUserCommand}" HorizontalAlignment="Right" Grid.Row="4" Margin="0,0,0,15" />
    </Grid>
</UserControl>
