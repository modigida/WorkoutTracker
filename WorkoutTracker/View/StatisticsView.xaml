<UserControl x:Class="WorkoutTracker.View.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkoutTracker.View" xmlns:viewmodel="clr-namespace:WorkoutTracker.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:MainWindowViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">
            <TextBlock Text="Statistics for " FontSize="24"/>
            <TextBlock Text="{Binding StatisticsVM.User.UserName}" FontSize="24"/>
        </StackPanel>

        <ComboBox 
            ItemsSource="{Binding StatisticsVM.TimeFrames}"
            SelectedItem="{Binding StatisticsVM.CurrentTimeFrame, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            Grid.Row="1" HorizontalAlignment="Center" Width="100" Margin="5">
            <ComboBox.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#90F0F0F0" />
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#80414141" />
            </ComboBox.Resources>
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding }" Foreground="#414141"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Text="Circlediagram displaying most common exercise based on amount of sets or reps" Grid.Row="2" />

        <ComboBox 
            ItemsSource="{Binding StatisticsVM.FavoriteExercises}"
            SelectedItem="{Binding StatisticsVM.SelectedExercise, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            Grid.Row="3" HorizontalAlignment="Center" Width="100" Margin="5">
            <ComboBox.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#90F0F0F0" />
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#80414141" />
            </ComboBox.Resources>
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ExerciseName}" Foreground="#414141"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        
        <TextBlock Text="Total amount of sets, average total weight and average reps per set" Grid.Row="4" />

        <TextBlock Text="Circlediagram percent of goal reached in pr" Grid.Row="5" />

    </Grid>
</UserControl>
