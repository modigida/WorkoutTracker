<UserControl x:Class="WorkoutTracker.View.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkoutTracker.View" 
             xmlns:viewmodel="clr-namespace:WorkoutTracker.ViewModel" 
             d:DataContext="{d:DesignInstance Type=viewmodel:MainWindowViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="400">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top">
            <TextBlock Text="Welcome to Workout Tracker" FontSize="24" HorizontalAlignment="Center" Margin="0,80,0,10"/>
            <TextBlock Text="{Binding StartText}" FontSize="14" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
